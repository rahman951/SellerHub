spring.application.name=auth-service
spring.profiles.active=dev

# JPA/Hibernate (?????)
spring.jpa.open-in-view=false
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.show-sql=false

# Swagger (???? ??????????? springdoc)
# springdoc.api-docs.enabled=true
# springdoc.swagger-ui.enabled=true

# JWT (???????? ? ??????)
auth.jwt.secret=CHANGE_ME_SUPER_SECRET_256BIT
auth.jwt.access-expiration=900000        # 15 ????? ? ??
auth.jwt.refresh-expiration=604800000    # 7 ???? ? ??

# Mail (???????? ? ???????? ???? SMTP)
spring.mail.host=smtp.example.com
spring.mail.port=587
spring.mail.username=no-reply@example.com
spring.mail.password=CHANGE_ME
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.from=no-reply@example.com

# ??????
server.port=8081
